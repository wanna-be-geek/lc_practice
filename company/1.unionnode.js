//两个有序链表的合并
//懂得用一个空节点来处理链表头的判断和返回
const ListNode = (val,next) => {
    this.val = (val === undefined ? 0 : val)
    this.next = (next === undefined ? null : next)
}
const makeList = (array) {

    return list
}
const mergeTwoLists = (l1, l2) => {
    ListNode 
}